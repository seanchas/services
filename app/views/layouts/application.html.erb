<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="content-type" content="text/html; charset=utf-8">
        <%= stylesheet_link_tag "main", "services", "requisites", "orders", "forms", "buttons", "calendar", "sidebar", :cache => "micex_services" %>
        <%= javascript_include_tag "prototype", "i18n", "calendar", :cache => "micex_services" %>
    </head>
    <body>
        <script type="text/javascript" charset="utf-8">i18n.set_locale('<%= I18n.locale %>');</script>
        <div id="page_wrapper">
            <%= render "shared/headline" %>
            <%= render "shared/header" %>
            <%= render "shared/navigation" %>
            
            <div id="content_wrapper">
                <%= content_tag :h1, yield(:title) if title_visible? %>
                
                <%- flash.each do |key, message| -%>
                    <%= content_tag :div, message, :id => :flash, :class => key %>
                <%- end -%>

                <div id="sidebar">
                    <%= yield :sidebar %>
                </div>
                
                <div id="content">
                    <%= yield %>
                </div>
            </div>
        </div>
        
        <div id="footer_wrapper">
            <%= render "shared/footer" %>
        </div>
    </body>
</html>
