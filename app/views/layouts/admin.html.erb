<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="content-type" content="text/html; charset=utf-8" />
        <%= stylesheet_link_tag "admin", :cache => "passport_admin" %>
        <%= javascript_include_tag "prototype", "prototype-ujs", :cache => "passport_admin" %>
        <meta name="csrf-param" content="<%= request_forgery_protection_token %>" />
        <meta name="csrf-token" content="<%= form_authenticity_token %>" />
    </head>
    <body>
        
        <%= render "admin/shared/header" %>
        
        <div id="content">
            <%= content_tag :h1, yield(:title), :class => :page_title if title_visible? %>
            <%= yield %>        
        </div>
        
    </body>
</html>
