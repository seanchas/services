<%= render "shared/registration_required" unless authenticated? %>
<%= render "shared/requisite_required" unless authenticated_requisite? if authenticated? %>
<%= render "shared/requisite" if authenticated_requisite? %>
